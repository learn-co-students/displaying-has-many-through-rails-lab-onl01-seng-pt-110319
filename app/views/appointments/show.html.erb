Doctor's Appointment: <%= link_to @appointment.doctor_name, doctor_path(@appointment.doctor_id)  %>
Patient's Appointment: <%= link_to @appointment.patient_name, patient_path(@appointment.patient_id)  %>
<%= @appointment.appointment_datetime.strftime("%B %-d, %Y at %k:%M") %>
