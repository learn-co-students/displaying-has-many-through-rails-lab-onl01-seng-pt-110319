<ul>
<%= @doctor.appointments.each do |a| %>
<li><%= a.appointment_datetime.strftime("%B %d, %Y at %k:%M") %></li>
<li><%= link_to a.patient.name, patient_path(a.patient_id) %></li>

</ul>
<% end %>

