<h1>Patient's Appointment Times</h1>
<% @patient.appointments.each do |appt| %>
<%= appt.appointment_datetime.strftime("%B %d, %Y at %k:%M") %><br>
<%= link_to appt.doctor.name, doctor_path(appt.doctor) %>
<%end%>
